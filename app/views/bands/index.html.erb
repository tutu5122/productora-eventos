<p id="notice"><%= notice %></p>

<h1>Bands</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Miembros</th>
      <th>Primer Show</th>
      <th>Tipo Banda</th>
      <th>Cantidad Conciertos</th>
      <th>Público Total</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bands.each do |band| %>
      <tr>
        <td><%= band.name %></td>
        <td><%= band.members %></td>
        <td><%= band.first_show %></td>
        <td><%= band.band_type.humanize %></td>
        <td><%= band.concerts.size %></td>
        <td><%= band.all_assistants %></td>
        <td><%= link_to 'Show', band %></td>
        <td><%= link_to 'Edit', edit_band_path(band) %></td>
        <td><%= link_to 'Destroy', band, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Band', new_band_path %>
